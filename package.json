{
  "name": "web-portfolio-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -c \"cyan.bold,yellow.bold\" -n \"FE,BE\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "dev:fe": "npm run dev --workspace=frontend",
    "dev:be": "npm run dev --workspace=backend",
    "build": "prisma db push --schema=backend/prisma/schema.prisma && prisma db seed --schema=backend/prisma/schema.prisma && npm run build --workspace=frontend",
    "postinstall": "prisma generate --schema=backend/prisma/schema.prisma",
    "db:push": "prisma db push --schema=backend/prisma/schema.prisma && prisma db seed --schema=backend/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prisma": "^6.19.3"
  }
}
